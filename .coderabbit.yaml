# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
#
# Inherits the org defaults from the central CodeRabbit configuration. Without
# `inheritance: true` this file would replace them entirely, including the
# setting that lets CodeRabbit approve pull requests.

inheritance: true

# Reviews in English. The org default is Japanese.
language: en-US
